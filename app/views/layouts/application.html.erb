<!DOCTYPE html>
<html>
  <head>
    <title>Aaa</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="sidebar videomodal">
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>

    <%= yield %>

    <div class="video-modal" data-videomodal-target="modal" aria-hidden="true">
      <button class="video-modal__close" type="button" aria-label="Fermer" data-action="click->videomodal#close">×</button>
      <div class="video-modal__backdrop" data-action="click->videomodal#close"></div>
      <div class="video-modal__content" data-videomodal-target="content"></div>
    </div>

  </body>
</html>
